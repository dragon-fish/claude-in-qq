{
  "name": "claude-qq-bridge",
  "version": "0.0.1",
  "description": "Drive Claude Code from QQ private chat — remote tool approval, session control, scan-to-configure.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "onboard": "bun run src/onboard.ts",
    "pair": "bun run src/pair.ts",
    "typecheck": "tsc --noEmit",
    "notify": "bun run src/notify.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.229",
    "qrcode": "^1.5.4",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
